# Motion-Detection
  This project uses the default camera of your device and displays a box whenever motion is indicated.
  It also has the ability to recognize if a face is in the frame by labeling the box surronding the face
  with "Face". I used OpenCV for this project.
